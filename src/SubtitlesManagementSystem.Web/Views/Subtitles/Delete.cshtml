@model SubtitlesManagementSystem.Web.Models.Subtitles.ViewModels.DeleteSubtitlesViewModel

<section class="meetings-page" id="meetings" style="min-height: 100vh;">
    <div class="container">
        <div class="row">
            @if (TempData["SubtitlesErrorMessage"] != null)
            {
                <div class="alert alert-danger" role="alert">
                    @TempData["SubtitlesErrorMessage"]
                </div>
            }
            <div>
                <dl class="row bg-secondary">
                    <dt class="col-sm-2 text-dark bg-light">
                        @Html.DisplayNameFor(subtitles => subtitles.Name)
                    </dt>
                    <dd class="col-sm-10 text-white">
                        @Html.DisplayFor(subtitles => subtitles.Name)
                    </dd>
                </dl>

                <form asp-action="Delete">
                    <input type="submit" value="Delete" class="btn btn-danger my-5" />
                    <a asp-action="Index" class="btn btn-primary my-5 ml-5">Back to Subtitles List</a>
                </form>
            </div>
        </div>
    </div>
</section>
