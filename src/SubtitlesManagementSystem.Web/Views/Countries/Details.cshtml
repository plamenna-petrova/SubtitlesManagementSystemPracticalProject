@model SubtitlesManagementSystem.Web.Models.Countries.ViewModels.CountryDetailsViewModel

<section class="meetings-page" style="min-height: 100vh;">
    <div class="container">
        <div class="row">
            <dl class="row bg-secondary" style="padding-left: 0;">
                <dt class="col-sm-2 text-white bg-info" style="font-weight: 400">
                    @Html.DisplayNameFor(country => country.Name)
                </dt>
                <dd class="col-sm-10 text-white font-weight-normal">
                    @Html.DisplayFor(country => country.Name)
                </dd>
                <dt class="col-sm-2 text-white bg-info" style="font-weight: 400">
                    @Html.DisplayNameFor(country => country.CreatedOn)
                </dt>
                <dd class="col-sm-10 text-white font-weight-normal">
                    @Html.DisplayFor(country => country.CreatedOn)
                </dd>
                <dt class="col-sm-2 text-white bg-info" style="font-weight: 400">
                    @Html.DisplayNameFor(country => country.CreatedBy)
                </dt>
                <dd class="col-sm-10 text-white font-weight-normal">
                    @Html.DisplayFor(country => country.CreatedBy)
                </dd>
                <dt class="col-sm-2 text-white bg-info" style="font-weight: 400">
                    @Html.DisplayNameFor(country => country.ModifiedOn)
                </dt>
                <dd class="col-sm-10 text-white font-weight-normal">
                    @Html.DisplayFor(country => country.ModifiedOn)
                </dd>
                <dt class="col-sm-2 text-white bg-info" style="font-weight: 400">
                    @Html.DisplayNameFor(country => country.ModifiedBy)
                </dt>
                <dd class="col-sm-10 text-white font-weight-normal">
                    @Html.DisplayFor(country => country.ModifiedBy)
                </dd>
                <dt class="col-sm-2 text-white bg-info" style="font-weight: 400">
                    Related Film Productions
                </dt>
                <dd class="col-sm-10 text-white">
                    @if (Model.RelatedFilmProductions.Count() > 0)
                    {
                        <table class="table table-striped table-light table-hover">
                            <thead>
                                <tr>
                                    <th>Title</th>
                                    <th>Duration</th>
                                    <th>Release Date</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var filmProduction in Model.RelatedFilmProductions)
                                {
                                    <tr>
                                        <td>
                                            @filmProduction.Title
                                        </td>
                                        <td>
                                            @filmProduction.Duration
                                        </td>
                                        <td>
                                            @filmProduction.ReleaseDate.ToShortDateString()
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    }
                    else
                    {
                        <div>No Related Film Productions</div>
                    }
                </dd>
            </dl>
        </div>
        <div>
            <a asp-action="Index" class="btn btn-primary my-5">Back to List</a>
            <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-dark">Edit Country</a>
        </div>
    </div>
</section>
