@model SubtitlesManagementSystem.Web.Models.Screenwriters.ViewModels.DeleteScreenwriterViewModel

<section class="meetings-page" id="meetings" style="min-height: 100vh;">
    <div class="container">
        <div class="row">
            @if (TempData["ScreenwriterErrorMessage"] != null)
            {
                <div class="alert alert-danger" role="alert">
                    @TempData["ScreenwriterErrorMessage"]
                </div>
            }
            <div>
                <dl class="row bg-secondary">
                    <dt class="col-sm-2 text-dark bg-light">
                        @Html.DisplayNameFor(actor => actor.FirstName)
                    </dt>
                    <dd class="col-sm-10 text-white">
                        @Html.DisplayFor(actor => actor.FirstName)
                    </dd>
                    <dt class="col-sm-2 text-dark bg-light">
                        @Html.DisplayNameFor(actor => actor.LastName)
                    </dt>
                    <dd class="col-sm-10 text-white">
                        @Html.DisplayFor(actor => actor.LastName)
                    </dd>
                </dl>

                <form asp-action="Delete">
                    <input type="submit" value="Delete" class="btn btn-danger my-5" />
                    <a asp-action="Index" class="btn btn-primary my-5 ml-5">Back to Screenwriters List</a>
                </form>
            </div>
        </div>
    </div>
</section>
